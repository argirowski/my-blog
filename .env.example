# NextAuth.js — generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Docker: copy this file to `.env` in the project root, set NEXTAUTH_SECRET, then:
#   docker compose up --build
